import { NotFoundPage } from "@/components/not-found-page";

export default function ContactNotFound() {
	return (
		<NotFoundPage
			title="Contact not found"
			description="This contact doesn't exist or has been removed."
		/>
	);
}
